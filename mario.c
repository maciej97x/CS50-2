#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int height;
    do
    height = get_int("height ");
    while(height < 2);

    for(i = 0; i < height; i++);
    {
        for(HASH )
    }

}