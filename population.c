#include <cs50.h>
#include <stdio.h>

int main(void)
{
 int start;
 do
 start = get_int("Start ");
 while (start < 2);

int end;
do
end = get_int("End ");
while (end > 8);

int years = 0;
for(int i = 0; i == end;i++)
{
  (start  start + start / 3 - start / 4);
  {
    printf("Years %i ", years);
  }
}
}
