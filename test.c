#include <stdio.h>
#include <cs50.h>

int main(void)
{
    int height;
    do
    {
        height = get_int("Numbers of rows ");
    }
    while (height > 1 || height < 9);

    for ( = 0; hash <= he; hash++)
    {
        printf
    }
}