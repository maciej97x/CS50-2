#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int height = 0;
    do
    {
        height = get_int("Number ");
    }
    while (height < 1);

    for (int = 0; int < height; i++)
    {
    printf("#");
    }
}